<Window x:Class="FamilyTreeApp.UI.Windows.ExportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Preview" 
        Height="600" Width="800"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Export Preview" FontSize="18" FontWeight="Bold" 
                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
        
        <!-- Preview Area with scroll -->
        <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                Background="#1E1E1E">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="PreviewImage" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
            </ScrollViewer>
        </Border>
        
        <!-- Image Info -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock x:Name="ImageInfoText" Foreground="{DynamicResource SecondaryTextBrush}" 
                       VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Export Options and Buttons -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Export Format -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Format:" Foreground="{DynamicResource PrimaryTextBrush}" 
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="FormatComboBox" Width="100" SelectedIndex="0">
                    <ComboBoxItem Content="PNG" Tag="png"/>
                    <ComboBoxItem Content="SVG" Tag="svg"/>
                </ComboBox>
            </StackPanel>
            
            <!-- Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Export" Click="Export_Click" Width="80" Margin="0,0,8,0"/>
                <Button Content="Cancel" Click="Cancel_Click" Width="80"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
